import { TotalpassengersPipe } from './totalPassengers.pipe';

describe('TotalpassengersPipe', () => {
  it('create an instance', () => {
    const pipe = new TotalpassengersPipe();
    expect(pipe).toBeTruthy();
  });
});
